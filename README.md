I chose SQL as my DB because the data I deal with is very relational and doing a cross-reference between them with JOIN is very convenient and very useful

In addition, SQL is a very easy language to implement and use