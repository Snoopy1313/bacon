services:
  backend_server:
    build: .
    ports:
      - "5000:5000"
  
  web_server:
    build: ./bacon_web
    ports:
      - "5173:5173"

  rabbitmq:
    image: rabbitmq:latest
    container_name: rabbitmq
    restart: always
    ports:
      - 5672:5672
      - 15672:15672